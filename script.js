const title = $("simple-dialog-title");
const location = $("dialog-location");
const participants = $("dialog-participants");
const starts = $("dialog-starts");
const ends = $("dialog-ends");
const notes = $("dialog-notes");
const nextBtn = $("next-btn");

nextBtn.on("click", function() {
    
})

